[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: exercises live external APIs (TMDb, Open Library) over the network
filterwarnings =
    ignore::DeprecationWarning
