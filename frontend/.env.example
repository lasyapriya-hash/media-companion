# Frontend environment. Copy to .env.local for local development.
#
# The ONLY frontend env var: the public base URL of the backend API.
# NEXT_PUBLIC_ vars are inlined into the client bundle, so NEVER put a secret
# here. No TMDb / Google Books / Anthropic keys belong on the frontend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
